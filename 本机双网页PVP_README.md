# ArcShot 本机双网页 PVP v0.2

## 启动

1. 用 Cocos Creator 3.8.8 打开工程，执行一次 `web-desktop` 构建。
2. 双击工程根目录的 `启动本机双网页PVP.bat`。
3. 脚本会启动本机网页服务和 WebSocket 中继，并打开两个独立 Chrome 窗口。
4. 两边保持房间号 `123456`，分别点击“匹配”。先进入者为 HOST/RED，后进入者为 GUEST/BLUE。

停止服务时双击 `停止本机双网页PVP.bat`。

## 固定地址

- 客户端：`http://127.0.0.1:8080/?pvp=1`
- 中继：`ws://127.0.0.1:8081`
- 中继诊断：`http://127.0.0.1:8081/diagnostics`
- 协议：`arcshot-pvp-local/0.2`
- 规则：`arcshot-local-20260821-r1`

## 调试开关

进入房间后会显示两个默认关闭的按钮：

- `重复COMMIT`：把下一条已收到的 `SHOT_COMMIT` 再交给客户端一次，验证不会重复启动物理。
- `Guest失步`：仅在 GUEST 下一次候选检查点生成后执行 `R1.xQ += 1000`；服务器应发送 `STATE_SYNC`，双方采用 HOST 检查点。

延迟按钮可在匹配前循环选择 `0 / 100 / 300 / 500 ms`。

## 当前限制

这是同一台电脑的双网页原型，中继只监听 `127.0.0.1`。局域网跨设备连接留到下一阶段。
